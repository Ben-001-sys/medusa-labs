export enum B2BAuditActorType {
  CUSTOMER = "customer",
  ADMIN = "admin",
  SERVICE = "service",
  EXTERNAL_SYSTEM = "external_system",
}

export enum B2BAuditOutcome {
  SUCCESS = "success",
  DENIED = "denied",
  FAILURE = "failure",
}